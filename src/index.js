import React from "react";
import ReactDOM from "react-dom";
import Weather from "./Weather";

const Index = () => {
    return <div>Hello React!</div>;
};

ReactDOM.render(<Weather />, document.getElementById("index"));